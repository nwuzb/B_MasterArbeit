# latexmk configuration file
# Set output directory for auxiliary files
$out_dir = '.output';

# Enable cleaning of auxiliary files
$clean_ext = 'aux bbl blg fdb_latexmk fls log out synctex.gz toc chktex pdfxref stderr stdout';

# Force recompilation when needed
$force_mode = 1; 